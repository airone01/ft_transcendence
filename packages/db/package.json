{
  "name": "@transc/db",
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "build": "docker compose build",
    "dev": "docker compose up",
    "lint": "$(../../biome.sh) check .",
    "format": "$(../../biome.sh) check . --fix"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "private": true
}
